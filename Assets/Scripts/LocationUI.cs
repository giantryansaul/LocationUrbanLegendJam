using UnityEngine;

public class LocationUI : MonoBehaviour
{
    // Location UI elements go here.
    // This should manage how the name, image, description and action choices should get handled in the UI.
}